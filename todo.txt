- authentication
- index multiple chains
x sql api
- live updates
- web view for issuing queries

SQL API

- validate user query
- generate ctes based on schema/user query

> POST /query

> [{
>     "chain_id": 84532,
>     "schemas": [{"signature": "Transfer(address indexed from,address indexed to, uint tokens)", "address": "0xabc"}]
>     "query": "select "from", tokens from transfer limit 1",
> }]

< {
<     "block_height": XXX,
<     "result": [
<         ["from", "tokens"],
<         ["0xaaa", "123"],
<         ["0xbbb", "456"]
<     ]
< }

Live Update API

> SSE /live-query

> {
>     "schemas": [{"signature": "Transfer(address indexed from,address indexed to, uint tokens)"}]
>     "query": "select value from, tokens transfer limit 1",
>     "block_height": XXX,
> }
